# Root requirements.txt for Railway auto-detection
# This file helps Railway detect this as a Python project

# The actual requirements are in backend/requirements.txt
# This is just for Railway's auto-detection
